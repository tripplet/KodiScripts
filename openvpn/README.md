Openvpn connection seems to fail if dns is not available if it tries to early and will continue to fail. Therefor the *wait-for-dns* service.

Service should be put into the target: *kodi.target.wants*